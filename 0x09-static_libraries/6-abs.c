#include "main.h"

/**
 * _abs - function that compute abolute of a number
 * @n: number to be checked
 *
 * Return: n if number is postive .else negate return the number
 */

int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (-1 * n);


}
